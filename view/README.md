# Build Setup

``` bash
# install dependencies 安装依赖
npm install

# serve with hot reload at localhost:8089  运行dev环境
npm run dev

# build for production with minification  编译
npm run build

# build for production and view the bundle analyzer report  发布
npm run build --report
```
